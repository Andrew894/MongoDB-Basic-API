services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/workspace
    command: node src/server.js
    depends_on:
      - db

  db:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: